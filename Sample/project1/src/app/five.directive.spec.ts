import { FiveDirective } from './five.directive';

describe('FiveDirective', () => {
  it('should create an instance', () => {
    const directive = new FiveDirective();
    expect(directive).toBeTruthy();
  });
});
